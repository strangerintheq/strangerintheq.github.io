
{
  "name": "coco-cesium",
  "version": "1.0.0",
  "description": "coco core cesium based",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stranger in the Q",
  "dependencies": {
    "cesium": "^1.34.0"
  },
  "devDependencies": {
    "gulp-task-builder": "git+https://github.com/strangerintheq/gulp-task-builder.git#1.0.7"
  }
}
